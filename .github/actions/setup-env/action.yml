name: 'Setup Environment'
description: 'Common Xcode setup used across all workflows'

runs:
  using: 'composite'
  steps:
    - name: Check Xcode version
      shell: bash
      run: xcodebuild -version

    # Uncomment if you need to select a specific Xcode version
    # - name: Select Xcode 26.0
    #   shell: bash
    #   run: |
    #     sudo xcode-select -s /Applications/Xcode_26.0.app
    #     xcodebuild -version

    # Uncomment if iOS Simulator is not pre-installed on macos-26
    # - name: Install iOS Simulator
    #   shell: bash
    #   run: |
    #     xcrun simctl list runtimes
    #     xcodebuild -downloadPlatform iOS
